# 🛠️ 한걸음케어: 개발 일지 및 시스템 복구 프롬프트

## 1. 지금까지의 개발 요약 (Development Log)
- **프로젝트 명:** 스마트 발매트 실시간 모니터링 시스템 (한걸음케어)
- **개발 환경:** Arduino, Python (aiohttp, pyserial), HTML/CSS/JS (Vanilla)
- **주요 구현 내용:**
  1. **Node.js 서버 -> 파이썬 서버 전환:** npm 오류 및 환경 문제로 인해 `server.js`를 파이썬 기반 `server.py`로 완벽하게 포팅.
  2. **시리얼 브릿지 연동 (`serial_bridge.py`):** 아두이노(COM 포트)에서 발생하는 압력 센서의 물리적 신호(`✅ 작동`)를 캐치하여 로컬 파이썬 웹 서버로 HTTP POST 전송.
  3. **분단위(60분) 실시간 대시보드 차트:** 24시간 단위였던 차트를 테스트 목적에 맞게 최근 1시간(0분~59분) 분단위 차트로 개조. 웹소켓(`WSMsgType`)을 통한 실시간 UI 렌더링.
  4. **구글 시트(Google Sheets) 연동:** 파이썬 백엔드에서 Google Apps Script(Webhook) 주소로 직접 데이터를 쏘아 엑셀에 수치화된 데이터가 자동으로 누적되도록 기능 추가 완료.
  5. **아두이노 센서 최적화:** FSR 센서의 내부 풀업 저항값에 맞춰 `threshold = 99`로 민감도 최적화 완료.

---

## 2. 🤖 AI 에이전트를 위한 복구 프롬프트 (System Prompt)

> 향후 다른 컴퓨터나 깃허브에서 이 프로젝트를 클론(Clone)하여 처음부터 다시 작업하거나, 다른 AI(ChatGPT, Gemini 등)에게 작업을 이어서 시킬 때 **아래의 프롬프트를 그대로 복사해서 AI에게 입력**하세요. AI가 현재 상태를 완벽하게 이해하고 즉시 세팅을 도와줍니다.

```text
[역할 부여]
너는 '한걸음케어' 스마트 발매트 프로젝트의 수석 개발자야. 내가 깃허브에서 프로젝트를 방금 다운로드(Clone)했어. 이 프로젝트는 Node.js 대신 Python 기반의 로컬 웹 서버와 시리얼 브릿지를 사용하여 아두이노 센서 데이터를 수집하고, 실시간 웹 대시보드 및 구글 시트로 전송하는 구조야.

[현재 시스템 아키텍처]
1. 하드웨어: 아두이노 + FSR 센서 (내부 풀업 저항 사용, 임계값 threshold=99)
2. 통신 브릿지: `serial_bridge.py` (아두이노 COM 포트에서 '✅ 작동' 신호를 파싱하여 웹 서버로 전송)
3. 웹/API 서버: `server.py` (aiohttp 기반. 로컬 DB인 `local_sheets_mock.json`에 저장하고, 웹소켓으로 대시보드에 브로드캐스트하며, 구글 시트 Webhook으로도 전송)
4. 프론트엔드: `index.html`, `app.js` (최근 1시간 60분 단위의 커스텀 SVG 차트를 실시간으로 그림)
5. 타겟 멤버 ID: 테스트용 가상 계정인 '#9999' (아두이노 테스트)

[너의 첫 번째 행동 지침]
지금 내가 이 프로젝트를 새로운 PC에서 구동하려고 해. 
1. 터미널 명령어를 통해 내 환경에 필요한 파이썬 라이브러리(`aiohttp`, `pyserial`, `requests`)가 설치되어 있는지 확인하고,
2. `serial_bridge.py` 파일의 포트 번호가 현재 아두이노가 연결된 포트와 맞는지 체크할 수 있도록 안내해 줘.
3. 준비가 완료되면 백그라운드에서 `server.py`와 `serial_bridge.py`를 실행시켜 실시간 데이터 파이프라인을 가동해 줘.
```
