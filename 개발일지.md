# 한걸음케어 개발 일지

---

## 📅 2026-06-04 작업 내용

### ✅ 완료된 작업

#### 1. GitHub 저장소 클론 및 프로젝트 세팅
- 팀원 UI 코드 GitHub (`kimdonguk01/puda`) 클론
- `c:\Users\PC-1\Desktop\한걸음케어\` 폴더에 병합
- Node.js 의존성 설치 (`npm install`)
- 로컬 서버 실행 (`node server.js` → `localhost:3000`)

#### 2. 아두이노 압력센서 데이터 파이프라인 설계 및 구현

**파이프라인 구조:**
```
[아두이노 FSR 센서]
  → USB 시리얼
    → Python 브릿지 (serial_bridge.py)
      → Node.js 서버 POST /api/sensor/data
        → 구글 시트 기록
          → hourly 집계 계산
            → WebSocket CHART_SYNC 브로드캐스트
              → 브라우저 24시간 활동 빈도 차트 실시간 갱신
```

#### 3. 구글 시트 연동 완료
- Google Cloud 프로젝트: **One Step Care**
- 서비스 계정: `id-22-454@one-step-care.iam.gserviceaccount.com`
- 스프레드시트 ID: `1SLlgY1ccP6EdR3Eyk9dQ1zNu6rUlSgNO3QG6nPKtEPo`
- 시트명: `센서데이터`
- 헤더: `Timestamp | MemberID | Step_Count | Left_Pressure | Right_Pressure | Balance_Score`
- `credentials.json` 프로젝트 루트에 배치 완료
- API 상태: ✅ **`google_sheets_v4_api` 모드로 정상 작동 확인**

#### 4. 테스트 이용자 생성
- ID: `#9999` / 이름: `아두이노 테스트`
- 페이지 로드 시 자동 생성 (localStorage에 없을 경우)
- 목적: 아두이노 실제 센서 데이터 수신 테스트 전용 계정

#### 5. 핵심 버그 수정
| 버그 | 원인 | 수정 내용 |
|---|---|---|
| 차트가 전혀 안 올라감 | `memberIndex` 변수 스코프 오류 (WebSocket 핸들러) | 변수 선언을 블록 최상단으로 이동, 통합 처리 |
| 시간대 오류 (UTC vs KST) | `getHours()` UTC 기준 계산 | `(getUTCHours() + 9) % 24` 로 KST 변환 |
| 시트 이름 오류 | `시트1` 하드코딩 | `센서데이터` 로 일괄 수정 |
| 서버 집계 지연 (12시간) | 스케줄러가 12시간 주기로만 CHART_SYNC 발송 | 센서 POST 수신 즉시 hourly 집계 후 실시간 CHART_SYNC 발송 |
| 페이지 로드 시 차트 0 | localStorage 오염값 | 상세 페이지 열 때 서버 `/api/sensor/hourly/:memberId` 에서 항상 최신 데이터 fetch |
| 랜덤값이 실제 데이터 덮음 | `getHourlyAggregations()` 빈 시간대에 랜덤값 삽입 | 빈 시간대는 0으로 처리 |

#### 6. 신규 파일 생성
| 파일 | 역할 |
|---|---|
| `serial_bridge.py` | 아두이노 시리얼 데이터를 Node.js 서버로 전달하는 Python 브릿지 |
| `test_sensor.js` | 아두이노 없이 서버 API를 직접 테스트하는 Node.js 스크립트 |
| `check_creds.js` | credentials.json 정보 확인용 임시 스크립트 (삭제 가능) |
| `google_sheets_config.json` | 구글 스프레드시트 ID 설정 파일 |

---

## 🔴 내일 이어서 할 작업 (우선순위 순)

### 1순위: 아두이노 실제 물리 센서 연동 테스트
- **아두이노 IDE 시리얼 모니터 닫기** (포트 충돌 방지)
- `serial_bridge.py` 상단 `PORT = 'COM3'` 을 실제 포트로 수정
- `pip install pyserial requests` 라이브러리 설치
- `python serial_bridge.py` 실행
- FSR 센서 누르면 → 구글 시트 기록 + 차트 막대 21시에 증가 확인

### 2순위: 차트 시각 개선
- 현재 `stepCount=1` 데이터가 너무 작게 보임
- 감지 1회당 최소 막대 높이 보장 로직 추가 (예: 최소 10 이상)
- 혹은 `stepCount` 스케일링 방식 개선

### 3순위: 폴링 방식 추가 (WebSocket 보완)
- WebSocket이 끊겨있을 때를 대비
- 상세 페이지 열려있는 동안 5초마다 `/api/sensor/hourly/:memberId` 자동 요청
- 차트 자동 갱신

### 4순위: 데모 시나리오 정리
- 발표/시연용 시나리오 스크립트 작성
- 아두이노 → 매트 밟기 → 실시간 차트 반응 흐름 시연

### 5순위 (장기): ESP8266 무선 버전 전환
- 아두이노 + ESP8266 Wi-Fi 모듈 연동 코드 작성
- `serial_bridge.py` 불필요해짐 (무선으로 직접 서버에 POST)
- 서버를 공인 IP/도메인에 배포

---

## 🔧 현재 서버 실행 방법

```bash
cd "C:\Users\PC-1\Desktop\한걸음케어"
node server.js
```
→ `http://localhost:3000` 접속

## 🧪 센서 데이터 테스트 방법 (아두이노 없이)

```bash
cd "C:\Users\PC-1\Desktop\한걸음케어"
node test_sensor.js
```
→ 브라우저에서 `#9999 아두이노 테스트` 상세 페이지 확인

---

## 📁 주요 파일 구조

```
한걸음케어/
├── server.js              # Node.js 백엔드 서버 (WebSocket + REST API)
├── app.js                 # 프론트엔드 메인 로직 (차트, 이용자 상태관리)
├── index.html             # 메인 HTML
├── index.css              # 스타일시트
├── googleSheetsDb.js      # 구글 시트 연동 DB 레이어
├── intervalManager.js     # 집계 스케줄러
├── serial_bridge.py       # 아두이노 → 서버 파이썬 브릿지
├── test_sensor.js         # API 직접 테스트 스크립트
├── credentials.json       # 구글 서비스 계정 키 (⚠️ git에 올리면 안됨!)
├── google_sheets_config.json  # 스프레드시트 ID 설정
├── local_sheets_mock.json # 구글 시트 연결 안 될 때 로컬 폴백 DB
├── FSR_Test/
│   └── FSR_Test.ino       # 아두이노 FSR 압력 센서 코드
└── package.json
```

> ⚠️ **보안 주의**: `credentials.json` 은 `.gitignore`에 반드시 포함되어야 합니다!
